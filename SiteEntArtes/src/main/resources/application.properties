# --- CONFIGURAÇÃO DA APLICAÇÃO ---
spring.profiles.active=local
spring.application.name=SiteEntArtes

# --- LIGAÇÃO À BASE DE DADOS (MySQL) ---
# Usamos o caminho direto para evitar erros de variáveis de ambiente
spring.datasource.url=jdbc:mysql://localhost:3306/entartes
spring.datasource.username=root
spring.datasource.password=Minicraques04

# --- CONFIGURAÇÃO JPA / HIBERNATE ---
# 'update' é mais seguro para testes, pois cria tabelas se elas não existirem
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect

# --- SEGURANÇA (JWT e HASHING) ---
# Valores fixos para garantir que o projeto arranca sem variáveis de sistema
JWT_SECRET=404E635266556A586E3272357538782F413F4428472B4B6250645367566B5970
HASH_SECRET=aminha_chave_secreta_para_ids_123
application.security.jwt.secret-key=404E635266556A586E3272357538782F413F4428472B4B6250645367566B5970
application.security.jwt.expiration=86400000

# --- CONFIGURAÇÃO DE EMAIL (GMAIL) ---
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=escolaentartesbraga@gmail.com
# Se o envio de email falhar, precisas de gerar uma "App Password" no Google
spring.mail.password=SuaAppPasswordAqui
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# --- OUTRAS CONFIGURAÇÕES ---
# Permite que o servidor aceite ficheiros maiores (útil para imagens/docs)
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB


